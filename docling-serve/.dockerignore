# Build context for docling-serve. `COPY . /app/` needs docling_api.py + the
# pyarmor_runtime_* package (protected build). Keep secrets and junk out.
machine_key.txt
*_token.txt
.env
.env.*
__pycache__/
*.pyc
*.pyo
.git
*.md
*.log
